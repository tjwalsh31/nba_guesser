[project]
name = "nba-guesser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tyler Walsh", email = "tjwalsh@udel.edu" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
nba-guesser = "nba_guesser:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
